interface:
  short_description: "Audit work-item validity before implementation"
  default_prompt: "Use $issue-preflight to audit whether this work item is still valid before implementing."

policy:
  allow_implicit_invocation: false
