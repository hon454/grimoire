interface:
  short_description: "Translate using the Grimoire session locale"
  default_prompt: "Use $magical-translation to translate this into my configured Grimoire locale."

policy:
  allow_implicit_invocation: true
