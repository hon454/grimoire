interface:
  short_description: "Recommend the next current-work action"
  default_prompt: "Use $now-what to inspect this workspace and recommend what to do next."

policy:
  allow_implicit_invocation: false
