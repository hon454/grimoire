interface:
  display_name: "Pre-Merge Tracker Sync"
  short_description: "Reconcile merge-ready changes with trackers"
  default_prompt: "Use $pre-merge-tracker-sync to reconcile this merge-ready change request with its tracker issues before merge."

policy:
  allow_implicit_invocation: false
